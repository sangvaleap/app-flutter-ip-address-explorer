import 'package:ip_checker/features/map/model/map_model.dart';

class GoogleMapModel extends MapModel {
  const GoogleMapModel(
      {required super.name, required super.imageUrl, super.providerUrl});
}
